# GoshaCrash 3.10.2-rc15

BT10 real-hardware fix:
- rc14 modern preflight ran before prepare_path.
- /usr/sbin/iptables therefore existed but could be invisible to command -v.
- rc15 prepares PATH first and probes standard absolute ASUSWRT paths.
- TUN runtime loading uses absolute modprobe/insmod paths as well.
