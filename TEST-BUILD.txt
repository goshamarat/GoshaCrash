# GoshaCrash 3.10.2-rc16

Real BT10 bugfixes:
- explicit /bin/sh for CLI wrapper and user-facing install commands;
- modern runtime firmware-first PATH;
- verified persistent gc wrapper;
- DM/ipkg packages: nano, unzip, openssh-sftp-server;
- one package-index refresh on modern DM;
- xterm-256color fallback for old DM nano;
- SFTP state/path detection fixed for physical DM_ROOT and /tmp/opt.
